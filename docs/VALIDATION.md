# 发布前检查记录

2026-09-08，首次公开整理版本。

| 检查 | 结果 |
| --- | --- |
| Switch GLB 组件身份 | 654 个，均与最终 FCStd 清单匹配 |
| Switch 2 GLB 组件身份 | 799 个，均与最终 FCStd 清单匹配 |
| GLB / FCStd SHA-256 关联 | 通过 |
| Khronos glTF Validator | 两个文件均 0 errors / 0 warnings |
| 原生 FCStd 文件 ZIP / CRC | 通过 |
| 发布脚本与宏语法 | 通过 |
| 移动后源码路径验证 | 两设备分别在新目录执行第 1–4 阶段，均无无效特征；包括接口、文字/标识等依赖 |
| 文档内部链接 | 根 README、设备 README、迭代索引、维护/许可文档无失效相对链接 |
| Vite 生产构建 | 通过，相对资源路径 |
| 桌面交互检查 | 1280 × 900，模型加载、爆炸程度、自动旋转、视角及设备切换 |
| 移动布局检查 | 375 × 812，无页面横向溢出，视图标签可横向滚动，模型随画布尺寸适配 |
| 模型外观检查 | 检查过原生 sRGB 颜色转换和硬边法线，网页模型保留设备配色 |

原始完整 CAD 的几何、全轮源码重建、干涉、STEP、图纸检查记录位于各设备 `output/reports/`。这些报告针对原始最终模型；本次整理保留原生二进制，仅调整可移植源码、文档和网页网格。

执行 `npm run check` 可重复文件、身份、语法和 glTF 检查。网页视觉和交互需要在真实浏览器中复核，不能由静态文件检查替代。
